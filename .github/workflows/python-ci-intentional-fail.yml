name: Python CI (Intentional Fail)

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

permissions:
  id-token: write
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    continue-on-error: false
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r app/requirements.txt

      - name: Run pytest (intentional fail)
        id: pytest
        continue-on-error: true
        env:
          PYTHONPATH: .:app
        run: |
          pytest | tee pytest-output.txt
        
      - name: Configure AWS credentials
        if: always()
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::${{ secrets.AWS_ACCOUNT_ID }}:role/github-actions-bedrock-role
          aws-region: us-east-1

      - name: Create failure_context.json
        if: always()
        run: |
          python scripts/analyze_failure.py pytest-output.txt failure_context.json

      - name: AI Analysis
        if: always()
        run: |
          python scripts/analyze_failure.py failure_context.json analysis-output.json
          cat analysis-output.json
          python scripts/print_markdown_summary.py analysis-output.json >> "$GITHUB_STEP_SUMMARY"

      - name: Fail if any previous step failed
        if: failure() && always()
        run: |
          echo "A previous step failed. Failing the pipeline after AI analysis."
          exit 1

      - name: Fail if tests failed
        if: env.PYTEST_EXIT_CODE != '0'
        run: |
          echo "Tests failed. Failing the pipeline after AI analysis."
          exit 1
